## Install dependencies

Open the project folder and install its dependencies.\
Gull React uses React Framework for development serve and build process

1. Make sure if [NodeJS](https://nodejs.org/en/download/) is installed on your computer.
2. Open terminal terminal and run `npm install -g create-react-app` to install React CLI globally
3. Then open the root project directory like /gull-mui-typescript and run `npm install` to install dependencies.

## Start development server

When the installation is done, you can now run your app.\
`npm start` or `yarn start`

## Build project files

Builds the app for production to the build folder. It correctly bundles React in production mode and optimizes the build for the best performance.\
`npm run build`

## Dependencies

- `@emotion/react`
- `@emotion/styled`
- `@mui/icons-material`
- `@mui/material`
- `@mui/x-data-grid`
- `@testing-library/jest-dom`
- `@testing-library/react`
- `@testing-library/user-event`
- `@types/jest`
- `@types/node`
- `@types/react`
- `@types/react-dom`
- `apexcharts`
- `axios`
- `axios-mock-adapter`
- `jsonwebtoken`
- `jwt-decode`
- `moment`
- `react`
- `react-apexcharts`
- `react-dom`
- `react-hook-form`
- `react-icons`
- `react-router`
- `react-router-dom`
- `react-scripts`
- `stylis-plugin-rtl`
- `typescript`
- `web-vitals`
